import {ReactElement} from "react";
import {ICSSAnimationProps} from "~/shared/CSSAnimation/index_d";

export interface IAnimationSliderViewportTemplateProps {
  children : ReactElement[];
  animation: ICSSAnimationProps
}
